docker run --name jenkins -p 8080:8080 -v /tmp:/var/jenkins_home -v /var/run/docker.sock:/var/run/docker.sock semyonov4360/jenkins

LocalHost:8080

docker exec -it jenkins bash

For PowerShell:

docker rm @(docker ps -aq)
